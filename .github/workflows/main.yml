name: "PROD DEPLOY"

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
    with:
      environment: prod
      aws-assume-role-arn: "arn:aws:iam::775322166386:role/GitHubAction-AssumeRoleWithAction"
      aws-region: "sa-east-1"
      aws-statefile-s3-bucket: "ae-edu-terraform-state-prod"
      aws-lock-dynamodb-table: "ae-edu-terraform-lock-prod"